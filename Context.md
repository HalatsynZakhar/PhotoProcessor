# PhotoProcessor - Технический контекст проекта

## 📌 Общий обзор

PhotoProcessor - это приложение для обработки фотографий и создания коллажей, реализованное на Python. Оно использует следующие основные технологии:

- **Streamlit** - для веб-интерфейса и управления состоянием
- **Pillow (PIL)** - для обработки изображений
- **JSON** - для хранения настроек и пресетов

Приложение рассчитано на пользователей, которым необходимо обрабатывать большие объемы фотографий для каталогов, интернет-магазинов и других целей. Основная цель - автоматизировать рутинные операции по обработке изображений.

## 🔍 Архитектура и ключевые компоненты

### Основные файлы и их назначение

1. **`app.py`**
   - Реализация пользовательского интерфейса на Streamlit
   - Управление состоянием приложения через `st.session_state`
   - Организация боковой панели с настройками
   - Запуск рабочих процессов обработки и отображение результатов

2. **`processing_workflows.py`**
   - Реализация основных рабочих процессов:
     - `process_individual_files()` - обработка отдельных файлов
     - `create_collage()` - создание коллажей
   - Оркестрация последовательности операций с изображениями
   - Управление путями файлов и логирование

3. **`image_utils.py`**
   - Низкоуровневые функции обработки изображений:
     - `whitening_perimeter()` - отбеливание периметра
     - `auto_crop()` - обрезка изображений
     - `apply_brightness_contrast()` - настройка яркости/контраста
     - `apply_borders()` - добавление полей
     - `resize_image()` - изменение размера
     - Другие служебные функции для работы с изображениями

4. **`config_manager.py`**
   - Управление настройками приложения
   - Сохранение/загрузка настроек из JSON-файлов
   - Управление наборами настроек (пресетами)

5. **`start.py`**
   - Скрипт для автоматического запуска приложения
   - Установка зависимостей из requirements.txt

### Поток данных и взаимодействие компонентов

1. **Загрузка и управление настройками**:
   - При запуске приложение инициализирует настройки по умолчанию
   - Настройки сохраняются в `settings.json` и наборах (пресетах) в каталоге `settings_presets/`
   - Для сохранения/загрузки настроек используются функции из `config_manager.py`

2. **Потоки обработки изображений**:
   - Пользователь настраивает параметры в UI (`app.py`)
   - При запуске обработки вызывается соответствующая функция из `processing_workflows.py`
   - Рабочий процесс использует настройки для последовательного вызова функций из `image_utils.py`
   - Результаты отображаются в интерфейсе и сохраняются в выходную директорию

## 💡 Ключевые функции обработки изображений

### 1. Отбеливание периметра
- Функция `whitening_perimeter()` в `image_utils.py`
- Логика: заменяет пиксели по периметру изображения на белые, если они соответствуют пороговым значениям
- Настройки: порог интенсивности (0-255), процент периметра, который должен соответствовать условиям

### 2. Обрезка и удаление фона
- Функция `auto_crop()` в `image_utils.py`
- Алгоритм сканирует изображение с четырех сторон для обнаружения границ объекта
- Настройки: чувствительность, дополнительные отступы

### 3. Добавление полей
- Функция `apply_borders()` в `image_utils.py`
- Три режима: всегда добавлять, добавлять только если периметр белый, добавлять только если периметр НЕ белый
- Настройки: размер полей, цвет полей, условия применения

### 4. Изменение размера и холст
- Набор функций в `image_utils.py` для управления размерами
- Поддержка принудительного соотношения сторон
- Ограничение максимального размера
- Создание холста точного размера с расположением изображения

### 5. Создание коллажей
- Функция `create_collage()` в `processing_workflows.py`
- Алгоритм размещения изображений на общем холсте с учетом отступов
- Настройки: количество столбцов, отступы, соотношение сторон коллажа

### 6. Яркость и контраст
- Функция `apply_brightness_contrast()` в `image_utils.py`
- Преобразование яркости и контраста с сохранением альфа-канала
- Настройки: множители яркости и контраста

## 🔄 Типичные сценарии использования

### Сценарий 1: Обработка фотографий для каталога товаров
1. Пользователь выбирает режим "Обработка отдельных файлов"
2. Указывает папку с исходными фотографиями товаров
3. Настраивает:
   - Отбеливание фона (порог ~100-150)
   - Обрезку с удалением лишних краев
   - Добавление полей (если периметр белый)
   - Принудительное соотношение 1:1
   - Максимальный размер 1200px
4. Запускает обработку
5. Получает стандартизированные изображения для каталога

### Сценарий 2: Создание коллажа для презентации
1. Пользователь выбирает режим "Создание коллажей"
2. Указывает папку с фотографиями для коллажа
3. Настраивает:
   - 3 столбца
   - Отступы 20px
   - Соотношение сторон 16:9
4. Запускает создание коллажа
5. Получает единое изображение-коллаж для презентации

### Сценарий 3: Пакетная обработка с переименованием
1. Пользователь выбирает режим "Обработка отдельных файлов"
2. Настраивает необходимые параметры обработки
3. Включает опции:
   - Переименование файлов (с заданным префиксом)
   - Удаление оригиналов
4. Запускает обработку
5. Получает обработанные и переименованные файлы

## ⚙️ Текущие задачи разработки

1. **Улучшение UI/UX**:
   - Оптимизация расположения элементов в боковой панели
   - Добавление более информативных подсказок
   - Предпросмотр обработанных изображений (3x6 сетка)

2. **Оптимизация производительности**:
   - Обработка больших объемов изображений (>100 файлов)
   - Кэширование промежуточных результатов

3. **Расширение функционала**:
   - Улучшение алгоритма отбеливания
   - Правильная обработка файлов разных форматов
   - Расширенная система работы с пресетами

## 🐞 Известные проблемы и ограничения

1. При обработке большого количества изображений интерфейс может "замерзать"
2. Иногда возникают проблемы с удалением оригинальных файлов разных форматов
3. Алгоритм отбеливания требует точной настройки порога для разных типов фонов
4. Предпросмотр может отображать только ограниченное количество изображений
5. Некоторые элементы UI имеют дубликаты, что может вызывать ошибки Streamlit

## 📚 Технические детали

### Состояние приложения
- Управление состоянием через `st.session_state` в Streamlit
- Автосохранение настроек после успешного выполнения обработки
- Ручное управление настройками через UI

### Файловая система
- Структура каталогов настраивается пользователем (input, output, backup)
- Работа с относительными и абсолютными путями
- Обработка ошибок доступа к файлам

### Логирование
- Двойное логирование: в UI и в файл `app.log`
- Подробная информация о каждом шаге обработки
- Отслеживание ошибок и предупреждений